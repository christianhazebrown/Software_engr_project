To run make sure all packages and Node.js are installed on your local pc using npm install -g <package-name> 
more than one packages can be installed at once buy just adding whitespace between.

Then in the terminal enter "npm run dev" then navigate to localhost:3000 in a webbrowser

